@{
    ViewBag.Title = "Filmster samler de danske streaming-tjenester";
}
<section class="block quick-search">
    @Html.Partial("_SearchForm")
</section>

<section class="block">
    <h2>Alle Video On Demand film der kan streames i Danmark</h2>
    <p>
        Filmster samler alle Video on Demand tjenerster til rådighed og Danmark, og giver dig det bedste overblik over priser, tilgængelighed og Software.
        <br />Søg blandt mere end 5.000 film fordelt imellem 8 udbydere.
    </p>
    <p>
        Hvis du ikke har prøvet at streame film online før, kan du læse mere om hvordan man gør, eller prøve at se nogle af de gratis fil, der er til rådighed.
    </p>
</section>
<script>
    $(function () {
        $("#queryTextbox").autocomplete("/Filmster/autocomplete", {
            parse: function (items) {
                var parsed = [];
                for (var i = 0; i < items.length; i++) {
                    var item = items[i];
                    if (item) {
                        parsed[parsed.length] = {
                            data: item,
                            value: item.Title,
                            result: item.Title
                        };
                    }
                }
                return parsed;
            },
            formatItem: function (item) {
                return item.Title;
            },
            selectFirst: false
        }).result(function (event, item) {
            location.href = item.Url;
        });
    });
</script>